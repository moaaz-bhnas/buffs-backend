// import NodeGecoder, { BaseOptions, GenericOptions } from "node-geocoder";

// const options: BaseOptions & GenericOptions = {
//   provider: "mapquest",
//   apiKey: process.env.GEOCODER_API_KEY,
//   formatter: null,
// };

// const geocoder = NodeGecoder(options);

// export default geocoder;
